name = "kavbug-ai"
main = "worker.js"
compatibility_date = "2026-06-01"

# הגבלת CORS לאתר שלכם (מומלץ). שנו לדומיין שלכם או הסירו לקבלת "*":
[vars]
ALLOWED_ORIGINS = "https://it-freak.github.io"

# סודות — אל תכניסו לכאן! הגדירו דרך:
#   npx wrangler secret put ANTHROPIC_API_KEY
#   (אופציונלי) npx wrangler secret put PROXY_SHARED_SECRET
