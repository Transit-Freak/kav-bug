{
  "name": "kav-bug",
  "version": "0.0.0",
  "private": true,
  "description": "האפליקציה עצמה ללא-בנייה (index.html טוען React/Babel מ-CDN). package.json זה משמש *רק* לבדיקות האוטומטיות (test/).",
  "scripts": {
    "test": "npm run test:transpile && npm run test:mount",
    "test:transpile": "node -e \"const b=require('@babel/core');for(const f of ['components.jsx','app.jsx'])b.transformFileSync(f,{presets:['@babel/preset-react']});console.log('transpile OK')\" && node --check data.js && node --check scan-country.js",
    "test:mount": "node test/mount-smoke.js"
  },
  "devDependencies": {
    "@babel/core": "^7.24.0",
    "@babel/preset-react": "^7.24.0",
    "jsdom": "^24",
    "react": "^18",
    "react-dom": "^18"
  }
}
